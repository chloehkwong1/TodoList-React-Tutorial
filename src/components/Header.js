import React from "react"

const Header = () => {
    return (
        <header>
            <h1>Simple Todo App</h1>
            <h1>Please add to-dos item(s) through the input field</h1>
        </header>
    )
}

export default Header